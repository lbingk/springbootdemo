
book.core.poolsize=4
book.max.poolsize=8
book.queue.capacity=100
book.keepAlive.seconds=5
book.thread.name.prefix=bookThread

config.core.poolsize=4
config.max.poolsize=8
config.queue.capacity=100
config.keepAlive.seconds=5
config.thread.name.prefix=configThread


server.port=8080



# Redis数据库索引（默认为0）
spring.redis.database=0
# Redis服务器地址
spring.redis.host=127.0.0.1
# Redis服务器连接端口
spring.redis.port=6379
# Redis服务器连接密码（默认为空）
spring.redis.password=
# 连接池最大连接数（使用负值表示没有限制）
spring.redis.jedis.pool.max-active=8
# 连接池最大阻塞等待时间（使用负值表示没有限制）
spring.redis.jedis.pool.max-wait=-1
# 连接池中的最大空闲连接
spring.redis.jedis.pool.max-idle=8
# 连接池中的最小空闲连接
spring.redis.jedis.pool.min-idle=0
# 连接超时时间（毫秒）
spring.redis.timeout=10000


spring.datasource.name=master
spring.datasource.type=com.alibaba.druid.pool.DruidDataSource
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.datasource.url=jdbc:mysql://139.159.225.164:3306/erp2order_full?useSSL=false&useUnicode=true&characterEncoding=UTF-8
spring.datasource.username=dev_erp
spring.datasource.password=Pagoda@963
defaultReturnRuleId=1